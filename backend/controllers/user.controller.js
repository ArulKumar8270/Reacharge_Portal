// Placeholder controllers - to be implemented
export const getUserProfile = async (req, res) => {
  res.json({ success: true, message: 'Get user profile - Coming soon' });
};

export const updateUserProfile = async (req, res) => {
  res.json({ success: true, message: 'Update user profile - Coming soon' });
};

export const changePassword = async (req, res) => {
  res.json({ success: true, message: 'Change password - Coming soon' });
};

